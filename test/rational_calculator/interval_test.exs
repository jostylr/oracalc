defmodule RationalCalculator.IntervalTest do
  import RationalCalculator.Interval
  alias RationalCalculator.Interval, as: Ival
  alias RationalCalculator.Fraction, as: Frac
  use ExUnit.Case
  doctest RationalCalculator.Interval, import: true

  test "assert" do
    assert true
  end
end
